---
export interface Props {
  eyebrow?: string;
  titulo: string;
  subtitulo?: string;
  align?: 'left' | 'center';
}
const { eyebrow, titulo, subtitulo, align = 'center' } = Astro.props;
---
<div class={`mb-12 ${align === 'center' ? 'text-center' : ''}`}>
  {eyebrow && (
    <span class="inline-flex items-center gap-2 text-secondary text-xs font-bold uppercase tracking-widest mb-3">
      <span class="w-6 h-px bg-secondary inline-block"></span>
      {eyebrow}
      <span class="w-6 h-px bg-secondary inline-block"></span>
    </span>
  )}
  <h2 class="text-3xl sm:text-4xl font-black text-light">{titulo}</h2>
  {subtitulo && (
    <p class="text-accent mt-3 max-w-2xl leading-relaxed mx-auto">{subtitulo}</p>
  )}
</div>
